#include<stdio.h>
int main(void)
{
    int x,y;
    puts("输入两个整数.");
    printf("整数x:");scanf("%d",&x);
    printf("整数y:");scanf("%d",&y);
    printf("它们的和为%d,积是%d",x+y,x*y);
    return 0;
}
